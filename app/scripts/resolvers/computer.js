define(['vendor/lodash', 'resolver'], function (_, Resolver) {
    'use strict';

    var ComputerResolver = Resolver.extend({

        getNextMove: function () {

        }

    });

    return ComputerResolver;

});
