extends ../layouts/default

block content
  .container
    h1 Export Click Report
    p Select a month and year to extract
    .row
      .col-sm-8
        form(class='form-inline', action='/click-report-download', method='post')
          .form-group
   	    label From Date
	input(type='text', name='mdate', id='mdate')
   label To Date
input(type='text', name='mdate', id='mdate')
          button.btn.btn-large.btn-primary(type="submit") Export Monthly Clicks

block js
block js
  script.

   $(function(){
       function initDate() {
          $('#mdate').datepicker({dateFormat: 'yyyy-mm-dd'});
       }
   });

