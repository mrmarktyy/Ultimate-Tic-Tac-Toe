define(['vendor/backbone'],
function (Backbone) {
    'use strict';

    var Message = Backbone.Model.extend({

        defaults: {

        },

        initialize: function () {

        },

    });

    return Message;
});
